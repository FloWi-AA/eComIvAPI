{
  "name": "ecom-invoice-test-endpoint",
  "version": "1.0.0",
  "private": true,
  "description": "Render-ready test endpoint for the eCom invoice APIs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
