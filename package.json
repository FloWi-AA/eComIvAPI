{
  "name": "skidata-render-invoice-endpoint",
  "version": "0.1.0",
  "private": true,
  "description": "Render-ready SKIDATA eCom invoice endpoint",
  "type": "module",
  "main": "dist/src/server.js",
  "scripts": {
    "start": "npm run migrate && node dist/src/server.js",
    "migrate": "node dist/src/database/migrate.js",
    "test": "vitest run dist/tests"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "pdfkit": "^0.20.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
